performer: Armando Trovajoli
openers: Carlo Rustichelli
venue: Anthology Film Archives
address: 32 Second Avenue, New York
date: 2026-01-22
time: "7:00 PM"
cost: $30 / $35 door
details:
  - An evening in two parts, with an intermission.
  - Seating is first come.

scheme: night             # by name, from design.yaml's palette

# The Commons photo is portrait, which would bisect the page vertically; this
# pins the horizontal split so the series still shows a row flow, and anchors
# the crop at the top so the cover crop keeps the face.
grid:
  split: horizontal
image:
  cell: top               # top/bottom here; left/right on a vertical split
  h_align: center
  v_align: top
